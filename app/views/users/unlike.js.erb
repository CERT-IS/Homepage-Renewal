<% if @board.board_type.name.eql?("lines") %>
$('#likes_<%= @board.id %>').html("<%=j render 'board_line/like', board: @board %>");
$('#likes_count_<%= @board.id %>').html("<%= @board.likes.count %> Likes");
$('#likes_count_<%= @board.id %>').attr("data-original-title", "<%= @board.like_members.limit(3).map(&:realname).join(', ') %><%= ' 외 '+ (@board.likes.count - 3).to_s + '명' if @board.likes.count > 3 %>");
<% elsif @call.eql?("timeline") %>
$('#likes_<%= @board.id %>').html("<%=j render 'users/like', board: @board %>");
<% else %>
$('#likes_<%= @board.id %>').html("<%=j render 'boards/like', board: @board %>");
<% end %>
