<%= form_for(current_user, url: like_user_path(current_user), method: :post, remote: true, authenticity_token: true) do |f| %>
	<a href="javascript:{};" onclick="$(this).parent().submit();">
	    <i class="expand-list rounded-x fa fa-heart-o"></i>
	</a>
	<input type="hidden" name="board_id" value="<%= board.id %>" />
<% end %>